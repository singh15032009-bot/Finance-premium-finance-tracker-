[pytest]
testpaths = tests
markers =
    live: hits the real Yahoo Finance API (needs an internet connection)
    pg: needs a PostgreSQL test database (WEALTHTRACK_TEST_DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning
